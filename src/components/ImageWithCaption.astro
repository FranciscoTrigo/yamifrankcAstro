---
export interface Props {
  src: string;
  alt: string;
  caption: string;
}

const { src, alt, caption } = Astro.props;
---

<figure style="text-align: center;">
  <img src={src} alt={alt} />
  <figcaption style="margin-top: 0.5em; font-style: italic;">{caption}</figcaption>
</figure>
